
  # fit

  This is a code bundle for fit. The original project is available at https://www.figma.com/design/vdgSJfCzlw1ZN2MeBH4AMR/fit.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  